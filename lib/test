total = "64.25"
num_of_people = 4
price_per_person = total.to_f / num_of_people

missing_value = nil

missing_value.length


def calculate_interest (loan_amount)
  loan_amount * 0.0525
end

puts calculate_interest (12000)
